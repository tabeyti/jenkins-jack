#!/usr/bin/env groovy

@Library('tools')_

pipeline {
    agent none
    stages {
        stage ('Build') {
            agent any

            steps {
                sh  '''
                    npm install
                    npm run vscode:prepublish
                    npm test --silent
                    '''
            }
        }
    }
}
